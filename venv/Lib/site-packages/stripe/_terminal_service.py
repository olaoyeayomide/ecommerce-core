# -*- coding: utf-8 -*-
# File generated from our OpenAPI spec
from paystack._paystack_service import paystackService
from paystack.terminal._configuration_service import ConfigurationService
from paystack.terminal._connection_token_service import ConnectionTokenService
from paystack.terminal._location_service import LocationService
from paystack.terminal._reader_service import ReaderService


class TerminalService(paystackService):
    def __init__(self, requestor):
        super().__init__(requestor)
        self.configurations = ConfigurationService(self._requestor)
        self.connection_tokens = ConnectionTokenService(self._requestor)
        self.locations = LocationService(self._requestor)
        self.readers = ReaderService(self._requestor)
