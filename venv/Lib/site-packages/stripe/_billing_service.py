# -*- coding: utf-8 -*-
# File generated from our OpenAPI spec
from paystack._paystack_service import paystackService
from paystack.billing._alert_service import AlertService
from paystack.billing._credit_balance_summary_service import (
    CreditBalanceSummaryService,
)
from paystack.billing._credit_balance_transaction_service import (
    CreditBalanceTransactionService,
)
from paystack.billing._credit_grant_service import CreditGrantService
from paystack.billing._meter_event_adjustment_service import (
    MeterEventAdjustmentService,
)
from paystack.billing._meter_event_service import MeterEventService
from paystack.billing._meter_service import MeterService


class BillingService(paystackService):
    def __init__(self, requestor):
        super().__init__(requestor)
        self.alerts = AlertService(self._requestor)
        self.credit_balance_summary = CreditBalanceSummaryService(
            self._requestor,
        )
        self.credit_balance_transactions = CreditBalanceTransactionService(
            self._requestor,
        )
        self.credit_grants = CreditGrantService(self._requestor)
        self.meters = MeterService(self._requestor)
        self.meter_events = MeterEventService(self._requestor)
        self.meter_event_adjustments = MeterEventAdjustmentService(
            self._requestor,
        )
