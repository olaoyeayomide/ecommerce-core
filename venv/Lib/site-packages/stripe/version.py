# -*- coding: utf-8 -*-
from typing_extensions import TYPE_CHECKING
from warnings import warn

warn(
    """
    The paystack.version package is deprecated and will become internal in the future.
    Pleasse access the version via paystack.VERSION.
    """,
    DeprecationWarning,
)

if not TYPE_CHECKING:
    from paystack._version import (  # noqa
        VERSION,
    )
