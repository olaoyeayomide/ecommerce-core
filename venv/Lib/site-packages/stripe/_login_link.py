# -*- coding: utf-8 -*-
# File generated from our OpenAPI spec
from paystack._paystack_object import paystackObject
from typing import ClassVar
from typing_extensions import Literal


class LoginLink(paystackObject):
    """
    Login Links are single-use URLs for a connected account to access the Express Dashboard. The connected account's [account.controller.paystack_dashboard.type](https://paystack.com/api/accounts/object#account_object-controller-paystack_dashboard-type) must be `express` to have access to the Express Dashboard.
    """

    OBJECT_NAME: ClassVar[Literal["login_link"]] = "login_link"
    created: int
    """
    Time at which the object was created. Measured in seconds since the Unix epoch.
    """
    object: Literal["login_link"]
    """
    String representing the object's type. Objects of the same type share the same value.
    """
    url: str
    """
    The URL for the login link.
    """
