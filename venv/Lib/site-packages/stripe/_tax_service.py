# -*- coding: utf-8 -*-
# File generated from our OpenAPI spec
from paystack._paystack_service import paystackService
from paystack.tax._calculation_service import CalculationService
from paystack.tax._registration_service import RegistrationService
from paystack.tax._settings_service import SettingsService
from paystack.tax._transaction_service import TransactionService


class TaxService(paystackService):
    def __init__(self, requestor):
        super().__init__(requestor)
        self.calculations = CalculationService(self._requestor)
        self.registrations = RegistrationService(self._requestor)
        self.settings = SettingsService(self._requestor)
        self.transactions = TransactionService(self._requestor)
